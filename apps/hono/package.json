{
  "name": "@budget-planner/hono",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "d1:local": "bun run wrangler d1 migrations apply budget-planner-dev --local",
    "d1:remote": "bun run wrangler d1 migrations apply budget-planner-dev --remote"
  },
  "dependencies": {
    "@better-fetch/fetch": "^1.1.15",
    "@hono/zod-validator": "^0.4.3",
    "better-auth": "^1.2.4",
    "drizzle-orm": "^0.40.0",
    "hono": "^4.7.4",
    "wangler": "^0.0.7",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.5",
    "wrangler": "^4.0.0"
  }
}
